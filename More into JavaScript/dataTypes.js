// There are different dataTypes in JS :
// Number, String, Boolean, undefined, BigInt, Symbol

let x = BigInt(123);
let y = Symbol(Hello);


// Non Primitive DataType - Object
 const Object = {
    fullName : "Raju Mistri",
    age : 20,
    cgpa : 8.2,
    isPass: true,
 };

 //printing object values.

 console.log(student["fullName"]);
 console.log(student.age);
 
 // Updating the value inside object....
 student["age"] = student["age"] + 1;
 console.log(student.age);

